const configs: {
  port: number;
  connectionString: string;
  secret: string;
} = {
  port: 3000,
  connectionString: "mongodb://127.0.0.1:27017/user-management",
  secret: "",
};

export default configs;
